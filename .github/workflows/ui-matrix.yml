name: UI Framework Matrix

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  framework-builds:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - next-js
          - next-ts
          - vite-js
          - vite-ts
          - vue-js
          - vue-ts
          - astro-js
          - astro-ts
          - angular-ts
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Build starter and all 40 styles
        env:
          TMPDIR: ${{ github.workspace }}/.matrix-tmp
        run: |
          mkdir -p "$TMPDIR"
          npm --workspace cli run ui:matrix -- --target ${{ matrix.target }} --install --build
      - name: Upload failed generated projects
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: ui-matrix-${{ matrix.target }}
          path: .matrix-tmp/
          retention-days: 5
