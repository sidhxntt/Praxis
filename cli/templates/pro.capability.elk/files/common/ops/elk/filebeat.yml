filebeat.inputs:
  - type: filestream
    id: docker-containers
    parsers:
      - container: ~
    paths:
      - /var/lib/docker/containers/*/*-json.log
processors:
  - add_docker_metadata:
      host: unix:///var/run/docker.sock
output.logstash:
  hosts: ["logstash:5044"]
