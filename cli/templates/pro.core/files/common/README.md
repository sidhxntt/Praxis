# {{projectName}}

Generated by Praxis Pro. The committed defaults are safe for local development; production refuses missing secrets and host configuration.

## Generated profile

- Stack: `{{proStack}}`
- Explicitly selected: {{requestedCapabilities}}
- Automatically implied: {{impliedCapabilities}}
- Effective capabilities: {{resolvedCapabilities}}

## Local stack

1. Copy `.env.example` to `.env` and set local values.
2. Run `docker compose up --build`.
3. Check `/api/v1/health/live`, `/api/v1/health/ready`, and `/api/v1/health/startup`.

Review `docs/operations.md` before deploying.

That runbook covers API and worker operation, migrations, rollback, backup and restore, disaster recovery, secret rotation, scaling, troubleshooting, cost controls, and the production-readiness checklist. If Kubernetes or Terraform was selected, also read `k8s/README.md` and `infra/terraform/README.md` before deployment.
