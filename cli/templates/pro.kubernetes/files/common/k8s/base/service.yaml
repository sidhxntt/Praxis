apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  selector:
    app.kubernetes.io/name: {{projectName}}
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: {{backendPort}}
