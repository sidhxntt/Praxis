{
  "name": "praxis",
  "version": "1.4.1",
  "private": true,
  "workspaces": [
    "cli",
    "web"
  ],
  "scripts": {
    "docs:context": "node scripts/resolve-template-context.mjs",
    "docs:context:check": "node scripts/check-template-context.mjs",
    "docs:links": "node scripts/check-doc-links.mjs",
    "docs:test": "node --test tests/docs/*.test.mjs",
    "docs:wiki": "node scripts/render-github-wiki.mjs",
    "docs:check": "npm run docs:links && npm run docs:test && npm run docs:context:check && node scripts/render-github-wiki.mjs /tmp/praxis-wiki-check",
    "dev:cli": "npm --workspace cli run dev",
    "dev:web": "npm --workspace web run dev",
    "build:web": "npm --workspace web run build",
    "build:cli": "npm --workspace cli run build",
    "test:cli": "npm --workspace cli run test",
    "check:cli": "npm --workspace cli run check",
    "check:web": "npm run build:web",
    "check": "npm run check:cli && npm run check:web"
  }
}
